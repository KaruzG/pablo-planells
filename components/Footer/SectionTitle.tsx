import React from "react";

const SectionTitle = ({ children }: { children: React.ReactNode }) => (
    <p className="text-sm font-bold uppercase tracking-widest text-white/50 mb-6">{children}</p>
);

export default SectionTitle;
